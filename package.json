{
  "name": "rainbow-packages",
  "private": true,
  "packageManager": "pnpm@9.12.2",
  "scripts": {
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "publish-packages": "changeset publish",
    "check:styles": "node scripts/check-no-hardcoded-colors.mjs",
    "validate:themes": "node scripts/validate-themes.mjs",
    "test:tailwind": "node scripts/tailwind-smoke.mjs"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.1",
    "@tailwindcss/postcss": "^4.2.0",
    "postcss": "^8.4.38",
    "tailwindcss": "^4.2.0"
  }
}
